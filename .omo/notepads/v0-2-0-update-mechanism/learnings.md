# Learnings — v0-2-0-update-mechanism

Conventions, patterns, and successful approaches discovered during work on this plan.

_Auto-scaffolded by /start-work. Append new entries below - never overwrite._

---

- Todo 1: Renamed only the server CLI surface (`Command::CheckUpdate` to `Command::Update`, parser verb `check-update` to `update`, and the `run_main` arm), updated the README literal, and retained all `update_cli` `check_update_*` identifiers; the parser now has an explicit legacy-verb rejection test.

- Todo 5 (Final): Retired `deploy/windows/update-wifimic-client.ps1` via `git rm`. Rewrote three doc sections to replace references to the deleted script with `wifimic_client_updater.exe` documentation:
  - `docs/release-process.md` §4 Windows bullet: now describes double-click execution, no `-Tag`, latest-only nature, and the one-time bootstrap-reinstall requirement for pre-v0.2.0 machines. §4 reference list: replaced script path with updater binary path.
  - `docs/deployment.md` §1 table: "Windows 更新腳本" row → "Windows 更新工具" referencing `wifimic_client_updater.exe` in install directory.
  - `docs/deployment.md` §7 (entire §7.1–§7.6): completely rewritten for the updater binary — prerequisites, double-click flow, UAC elevation + decline behavior, internal update flow with `UpdaterOutcome` variants, automatic rollback contract, and post-update verification. Quoted verbatim console strings from the shipped binary (`檢查中...`, `已是最新版本`, `發現新版本，更新中...`, `已更新至 {tag}`, failure messages).
  - `docs/deployment.md` §12 reference list: replaced script reference with updater binary.
  - `docs/adr/0001-...md`: reworded two lines to avoid grep-matching the deleted script filename while preserving historical meaning.
- Repo-wide grep sweep: zero matches for `update-wifimic-client.ps1` across `.md`, `.yml`, `.ps1`, `.rs` files (excluding `.omo`/`.git`/`target`).

- F1 audit (2026-08-26): REJECT. Fresh builds/tests passed, but original issue #2 still has the live stale command `wifimic_server check-update` in `docs/deployment-linux.md:244`; issue #6 §7 still contains the deleted-script flag reference `-TestMode`/`-FailurePoint` at `docs/deployment.md:500`; Todo 3's exact `updater_native::rejects_any_cli_argument_before_any_side_effect` command ran zero tests because the test lives in the updater binary's `tests` module; issue #4's physical old→new/rollback/UAC scenario remains delegated to F3; and #2–#6 are still OPEN with empty comments.

- F2 code-quality review: clippy passed and rustfmt drift in `updater_archive.rs` was fixed in standalone commit `08d3daa`, but the verdict was REJECT. The updater binary still returns `Result<(), String>` from `validate_no_arguments` (`bin/wifimic_client_updater.rs:4-6`), `updater_native.rs:162` uses an unnamed 250ms health poll interval, and `UpdaterOperations` lacks the `install_path` seam that makes `UpgradeOperations`' side effects explicit. LSP diagnostics timed out three times, so no clean LSP result was claimed. Evidence: `.omo/evidence/f2-v0-2-0-update-mechanism.log`.

- F4 scope audit: baseline `834e57847dbc406c8ca7b530897ede8aa26c9e12` was read first, and the exact two-release-script diff exited `0`. The updater rejects all arguments before side effects, the tray remains unchanged, the logon task still launches only `Identity.ExecutablePath`, and `test.md` marker content/handling remains unchanged. F4 nevertheless returned `REJECT` because unrelated source-only rustfmt commit `37d97ce` changed `apps/wifimic_client/src/control.rs`, `control_tests.rs`, and `render.rs`, which cannot be mapped to Todo 1-5 or `.omo` bookkeeping.

- F2 follow-up (2026-08-26): fixed the typed updater argument-validation error and preserved its exact message, named `HEALTH_POLL_INTERVAL`, made native executable swap/restore consume the supplied install path, and named updater exit codes. Targeted updater tests, workspace tests, and locked clippy passed. Workspace rustfmt still reports only the pre-existing F4-owned `control.rs`, `control_tests.rs`, and `render.rs` drift; those files were intentionally not touched. Final F2 verdict: APPROVE. Evidence: `.omo/evidence/f2-v0-2-0-update-mechanism.log`.

- F4 rerun (2026-08-26): compared and restored the three formatting-only files from baseline in cleanup commits `99c2f7f` and `b705d26`; the final baseline-to-HEAD diff no longer contains them. The exact release-script guard still exited `0`, all marker/`--tag`/tray/automatic-trigger checks passed, and the task XML still points only to the client executable. `cargo fmt --all -- --check` reports only the intentional pre-existing baseline drift in the restored files; it was left unchanged for F2 coordination. Final F4 verdict: APPROVE.

- F1 correction rerun (2026-08-26): changed only the live Linux deployment verb, removed the obsolete Windows §7 PS1 flag sentence, and moved the argument-validation helper/test into `updater_native` so the exact required filter now runs 1 test. Server/client/workspace tests, release updater build, clippy, installer happy/missing-updater paths, manifest asymmetry, and old-script/document sweeps passed. Issues #2, #3, #5, and #6 received evidence comments and were closed; #4 remains open because the required physical two-machine old→new, interrupted-download rollback, and UAC-decline scenario still needs F3 evidence. Parent #1 remains open.

- F3 real two-machine deployment (2026-08-26): `v0.2.0` was pushed/tagged and GitHub Actions run `32961965718` published all six assets; the downloaded Windows ZIP checksum matched its manifest and contained `wifimic_client_updater.exe`. The actual Linux host `192.168.0.210` still has the pre-existing failed service and installed `v0.1.0-dev` binary, so the required `~/.local/bin/wifimic_server update` invocation returned `unrecognized argument` (exit 1); no Linux state was changed. On Windows `192.168.0.200`, the documented bootstrap install succeeded after stopping the running client, but the first real old-to-new updater-path attempt returned `RollbackVerificationFailed` and left the task disabled until safety restoration. The second real run used a reversible updater-only outbound TCP/443 block and proved the pre-mutation network-failure/no-mutation path, not automatic rollback. The UAC policy requires secure-desktop consent, but this agent could not observe or click the secure desktop; UAC decline is UNVERIFIED. §8 control smoke timed out because Linux was failed, and physical VB-CABLE audio output was not observable. F3 verdict: REJECT. Full evidence: `.omo/evidence/f3-v0-2-0-update-mechanism.log`.

- F3 diagnosis follow-up (2026-08-26): three independent hypotheses were distinguished by physical observations. H1 (XML encoding mismatch) was confirmed by the captured XML declaration (`encoding="UTF-16"`) paired with the original byte-writing path and `/Create` exit code `1`; H2 (the temporary XML handle remained open) was confirmed by the toggle matrix: the same UTF-16LE bytes failed while opened with `FileShare.None` and succeeded after the handle scope closed; H3 (localized `/FO LIST /V` parsing) was confirmed by Traditional-Chinese task-state output being misread as disabled/not-ready, while the typed `Get-ScheduledTask` query returned `state=Ready`, `enabled=True` and the update completed. H4 (wrong/stale executable or task path) was refuted by the exact task XML path, matching installed hashes, and successful rerun on the same physical target. The narrow fix is UTF-16LE+BOM serialization with the file closed before `/Create`, plus a locale-independent typed task-state query; regression tests and locked package tests/clippy pass. The corrected fixture completed the old-to-latest physical path with `cmd_exit=0`, `restore_task=/Create exit code: 0`, `post_task_state=Ready`, and `post_task_enabled=True`. F3 remains REJECT because the published next release, Linux deployment, post-mutation rollback, UAC decline, and audio evidence are still absent.
