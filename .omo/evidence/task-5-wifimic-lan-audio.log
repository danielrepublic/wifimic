Task 5 happy-path evidence
==========================

Codegraph limitation:
- The indexed workspace returned protocol symbols but no wifimic_server capture symbols. The server, micdriver adapter, frame chunker, and capture error references were inspected directly.

Passed:
- cargo test -p wifimic_server capture
  7 passed, 1 ignored (capture_adapter_starts_and_stops_real_source).
- cargo test -p wifimic_server missing_endpoint_is_a_typed_error_without_default_substitution -- --nocapture
  1 passed; typed EndpointNotFound assertion succeeded.
- cargo build --workspace
  passed.
- cargo clippy -p wifimic_server --all-targets -- -D warnings
  passed.
- cargo fmt -p wifimic_server -- --check
  passed; repository rustfmt emitted only the pre-existing unknown use_small_diffs warning.

Behavior covered by the passing capture suite:
- idle construction does not spawn;
- exact pinned parec argument list and source;
- partial stdout re-chunks into exact wifimic_protocol::PCM_PAYLOAD_BYTES frames;
- injected acquisition clock timestamps remain monotonic;
- stop is idempotent and releases the fake process seam;
- missing source is typed and never substituted;
- partial/empty stdout is typed malformed input.

Cleanup:
- Unit tests use in-memory process/reader seams only; no fake child process remains.

Capture facade refactor verification
-------------------------------------
- `cargo test -p wifimic_server capture -- --nocapture`: passed; 7 passed, 1 ignored.
- `cargo clippy -p wifimic_server --all-targets -- -D warnings`: passed.
- `cargo build --workspace`: passed after the extraction; workspace clippy remains blocked only by the unrelated client import below.
- `cargo fmt -p wifimic_server`: passed; repository rustfmt still reports the pre-existing client formatting difference when run workspace-wide.
- Pure LOC: `capture.rs` 102, `capture_types.rs` 88, `capture_reader.rs` 39, `capture_process.rs` 114; all are below the 250-LOC ceiling.
- The public `capture` facade still exports `CaptureHandle`, `CapturedFrame`, `CaptureError`, `PcmFrame`, `PINNED_CAPTURE_SOURCE`, and `PAREC_ARGUMENTS`; private types, frame reading, and process lifecycle are split behind module seams.
