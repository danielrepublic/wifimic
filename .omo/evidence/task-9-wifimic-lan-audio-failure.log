# Task 9 failure/limitation evidence
# Captured 2026-08-22T09:07:49Z from C:\Users\Daniel\Documents\opencode\wifimic.

## Clean-host live install not performed

arch-daniel already carries the Task 8 deployment state (unit installed, linger enabled,
binary built from origin/main=047e2cc2c1b9762e62753adeda74ceb17b91bf8c, UFW rules applied).
This guide documents that real, verified path but was NOT validated by wiping the host and
re-running every command from zero. A clean-host rehearsal would require a second machine
or destructive reset and was out of scope. The guide's section 11 states this limitation.

## systemd-analyze verify not re-run remotely

No ssh session was opened during this documentation task, so `systemd-analyze --user verify`
was not executed again against arch-daniel. The authoritative verification remains Task 8's
reconciled run (no output, exit 0) recorded in task-8-wifimic-lan-audio.log. The guide cites
behavior consistent with that run (203/EXEC troubleshooting entry, active-state expectation).

## nft ruleset syntax not machine-checked

`nft -c -f` requires the nft binary, unavailable on this Windows host; WSL is not installed.
deploy/linux/wifimic-server.nft was reviewed manually: its two rules match byte-for-byte the
grep assertions inside wifimic-server-firewall.sh apply_nft_rules() and the expected output
quoted in guide section 6.3.

## Third LAN source still unavailable

No third reachable LAN host exists to prove the port-scoped deny path live. Inherited from
Task 8: accept counter incremented once from 192.168.0.200; drop counter remained 0. The
guide's section 11 records this honestly and no fabricated source was invented.

## UDP reachability test is one-directional

The guide's Windows-side UdpClient probe cannot confirm delivery by itself (UDP, and the
current server main loop discards datagrams silently). Section 7.2 explicitly directs the
reader to Linux-side firewall counters as the authoritative confirmation.

## Draft defects caught before commit

1. Forbidden string `/home/daniel/.psw` appeared in a prohibition note; removed after the
   grep check flagged it (final count 0).
2. `Test-NetConnection -Udp` invalid command; replaced with UdpClient snippet.
3. Hard-coded UFW rule numbers in example output; replaced with numbering-independent text.

All three were found by the checks recorded in task-9-wifimic-lan-audio.log and fixed in
the same working session before staging.
