# Task 17 — Windows client updater deterministic happy-path evidence

Date: 2026-08-22
Script: `deploy/windows/update-wifimic-client.ps1`

## Deterministic injected-operation harness

The harness invoked PowerShell 5.1 with `-TestMode`, a unique child of the Windows
temporary directory, and no Scheduled Task, Program Files, process, firewall, or
driver operations. The fake operation state was removed after every invocation.

Command shape:

```powershell
powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File .\deploy\windows\update-wifimic-client.ps1 -Tag v1.0.0 -TestMode -TestStateRoot <new-temp-child>
```

Observed deterministic result:

```json
{"Status":"Updated","Mode":"Test","Tag":"v1.0.0","ResolvedCommit":"aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa","ExecutablePath":"C:\\Program Files\\wifimic-client\\wifimic_client.exe","PriorHash":"80814ad46f05d7ebad4774fdcf6b1acca39a9c4e873f0be5d5fed0634543d068","CandidateHash":"ed0f2c706004fc9f03aa50f874c6987687514014330afd35304f28822d5db914","InstalledHash":"ed0f2c706004fc9f03aa50f874c6987687514014330afd35304f28822d5db914","TaskPath":"\\wifimic\\wifimic-client","TaskEnabled":true,"TaskState":"Running","Endpoint":"CABLE Input (VB-Audio Virtual Cable)","PriorTaskEnabled":true,"PriorTaskWasRunning":true}
```

The fake event sequence proved the intended transaction order:

```text
GetSourceStatus, ResolveRevision, FetchTags, GetTask, GetRenderEndpointNames,
EnsureDirectory, EnsureDirectory, AddDetachedWorktree, BuildCandidate,
GetTask, GetRenderEndpointNames, WriteTransactionBytes, DisableTask, GetTask,
StopTask, GetTask, AtomicInstallFile, RestoreTask, GetTask, EnableTask,
StartTask, CheckHealth, RemoveDetachedWorktree, RemoveDirectoryTree,
RemoveDirectoryTree
```

Assertions passed:

- the explicit revision resolved before fetching, and the source status was clean;
- the candidate was detached/staged and built before task mutation;
- the installed hash changed to the candidate hash;
- the canonical task path remained `\wifimic\wifimic-client`, enabled and `Running`;
- health required the exact `CABLE Input (VB-Audio Virtual Cable)` endpoint;
- the fake staging, transaction, and state roots were absent after cleanup.

## Read-only live inspection

The host was inspected without mutation:

```json
{"ExactEndpointEnumerated":true,"EndpointMatches":["CABLE Input (VB-Audio Virtual Cable)"],"CanonicalTaskPresent":false,"TaskState":"Absent","TaskEnabled":false}
```

This confirms only the endpoint. No live task update, executable replacement,
firewall change, process termination, driver install, or logout was attempted.
