Task 16 failure-path receipt
Date: 2026-08-22

Argument and preflight failures (all exit 1, before mutation):
- No argument: `wifimic server update aborted: exactly one explicit Git tag or commit is required`.
- Extra argument (`one two`): same exact-argument rejection.
- Whitespace revision (`bad revision`): `revision must be a non-empty tag or commit, not an option or whitespace-containing value`.
- A missing valid-looking tag in the isolated clean harness attempted only the requested exact tag fetch, reported `fetch tag missing-tag failed or timed out` when the harness had no origin, and left the prior binary hash/service state unchanged.
- The real checkout's dirty orchestration state is rejected before any fetch/build/swap when a valid explicit revision is supplied.

Bounded failure cases in the isolated harness:
- `WIFIMIC_UPDATE_TIMEOUT_SECONDS=20` and `WIFIMIC_HEALTH_TIMEOUT_SECONDS=5` bounded all fake remote/build/service/smoke waits.
- Build command emitted `deliberate build failure`; the service remained `active` and the prior binary hash remained unchanged.
- Deliberately bad tag reached restart and active checks, then the smoke emitted `deliberate smoke failure`; the EXIT trap restored the prior binary and left the service `active`.
- The rollback assertion compared SHA-256 before/after and asserted active service state, rather than trusting a send or success string.
- Repeated cleanup assertions found no staging worktree or transaction directory; the temporary harness itself was deleted.

Security/cleanup receipt:
- No credentials, password files, firewall commands, alternate backends, checkout reset, force-push, or persistent remote staging artifacts were used.
- Operation output is temporary and redacts URL credentials/token/password patterns before failure output; the transaction directory is removed on success and failure.
